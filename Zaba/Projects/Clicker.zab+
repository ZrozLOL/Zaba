import window
local form = window:CreateNewForm("My App")
local btn = window:CreateButton("Click me")

btn.Pos = "50,100"
btn.Size = "150,40"
btn.Text = "Click!"
form.Text = "Cool App"
local clicks = 0

btn:OnClick[
    clicks = clicks + 1
    pring clicks
]
